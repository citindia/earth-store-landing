import { Metadata } from "next";

export const metadata: Metadata = {
	title: "Shop | Planet Earth Store",
	description: "Shop page of Planet Earth Store",
};

const page = () => {
	return (
		<>
			<div className="">Shop page</div>
		</>
	);
};

export default page;
